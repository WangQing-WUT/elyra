{
	"type": "object",
	"properties": {
		"name": {
			"title": "Name",
			"description": "A custom Name for the node.",
			"type": "string"
		},
		"label": {
			"title": "Label",
			"description": "A custom label for the node.",
			"type": "string"
		},
		"component_parameters": {
			"type": "object",
			"properties": {
				{% if  component.name == "Dataset Event" %}
				"dataset_event_parameters": {
					"type": "array",
					"title": "Dataset Event Parameters",
					"description": "Dataset event parameters.",
					"items": {
						"type": "object",
						"properties": {
							"id": {
								"type": "string",
								"title": "Id"
							},
							"name": {
								"type": "string",
								"title": "Name",
								"enum": ["DSEvent.name", "DSEvent.operate", "DSEvent.samples", "DSEvent.create_at", "DSEvent.publish_status", "DSEvent.subscribe_status"]
							}
						},
						"allOf": [
							{
								"if": {
        							"properties": {
          								"name": {
           					 				"const": "DSEvent.name"
     						  			}
       								}
      							},
								"then": {
        							"properties": {
          								"operate": {
            								"type": "string",
											"title": "Operate",
											"enum": ["=="]
         								},
										"value": {
											"type": "string",
											"title": "Value"
										}
       								},
        							"required": ["value"]
								}
							},
							{
								"if": {
        							"properties": {
          								"name": {
           					 				"const": "DSEvent.operate"
     						  			}
       								}
      							},
								"then": {
        							"properties": {
          								"operate": {
            								"type": "string",
											"title": "Operate",
											"enum": ["=="]
         								},
										"value": {
											"type": "string",
											"title": "Value"
										}
       								},
        							"required": ["value"]
								}
							},
							{
								"if": {
        							"properties": {
          								"name": {
           					 				"const": "DSEvent.samples"
     						  			}
       								}
      							},
								"then": {
        							"properties": {
          								"operate": {
            								"type": "string",
											"title": "Operate",
											"enum": [">", "<", "==", ">=", "<=", "!=" ]
         								},
										"value": {
											"type": "integer",
											"title": "Value"
										}
       								},
        							"required": ["value"]
								}
							},
							{
								"if": {
        							"properties": {
          								"name": {
           					 				"const": "DSEvent.create_at"
     						  			}
       								}
      							},
								"then": {
        							"properties": {
          								"operate": {
            								"type": "string",
											"title": "Operate",
											"enum": ["before", "after"]
         								},
										"value": {
											"type": "string",
											"title": "Value",
											"format": "datetime"
										}
       								},
        							"required": ["value"]
								}
							},
							{
								"if": {
        							"properties": {
          								"name": {
           					 				"enum": ["DSEvent.publish_status", "DSEvent.subscribe_status"]
     						  			}
       								}
      							},
								"then": {
        							"properties": {
          								"operate": {
            								"type": "string",
											"title": "Operate",
											"enum": ["=="]
         								},
										"value": {
											"type": "string",
											"title": "Value"
										}
       								},
        							"required": ["value"]
								}
							}
						]
					},
					"uihints":{
						"items": {
							"id": { "ui:readonly": "true" }
						}
					}
				},
				{% endif %}
			    {% if  component.name == "Model Event" %}
				"model_event_parameters": {
					"type": "array",
					"title": "Model Event Parameters",
					"description": "Model event parameters.",
					"items": {
						"type": "object",
						"properties": {
							"id": {
								"type": "string",
								"title": "Id"
							},
							"name": {
								"type": "string",
								"title": "Name",
								"enum": ["ModelEvent.name", "ModelEvent.operate", "ModelEvent.version", "ModelEvent.create_at", "ModelEvent.update_at"]
							}
						},
						"allOf": [
							{
								"if": {
        							"properties": {
          								"name": {
           					 				"const": "ModelEvent.name"
     						  			}
       								}
      							},
								"then": {
        							"properties": {
          								"operate": {
            								"type": "string",
											"title": "Operate",
											"enum": ["=="]
         								},
										"value": {
											"type": "string",
											"title": "Value"
										}
       								},
        							"required": ["value"]
								}
							},
							{
								"if": {
        							"properties": {
          								"name": {
           					 				"const": "ModelEvent.operate"
     						  			}
       								}
      							},
								"then": {
        							"properties": {
          								"operate": {
            								"type": "string",
											"title": "Operate",
											"enum": ["=="]
         								},
										"value": {
											"type": "string",
											"title": "Value"
										}
       								},
        							"required": ["value"]
								}
							},
							{
								"if": {
        							"properties": {
          								"name": {
           					 				"const": "ModelEvent.version"
     						  			}
       								}
      							},
								"then": {
        							"properties": {
          								"operate": {
            								"type": "string",
											"title": "Operate",
											"enum": ["=="]
         								},
										"value": {
											"type": "string",
											"title": "Value"
										}
       								},
        							"required": ["value"]
								}
							},
							{
								"if": {
        							"properties": {
          								"name": {
           					 				"enum": ["ModelEvent.create_at", "ModelEvent.update_at"]
     						  			}
       								}
      							},
								"then": {
        							"properties": {
          								"operate": {
            								"type": "string",
											"title": "Operate",
											"enum": ["before", "after"]
         								},
										"value": {
											"type": "string",
											"title": "Value",
											"format": "datetime"
										}
       								},
        							"required": ["value"]
								}
							}
						]
					},
					"uihints":{
						"items": {
							"id": { "ui:readonly": "true" }
						}
					}
				},
				{% endif %}
				{% if  component.name == "MinIO Event" %}
				"endpoint": {
					"type": "string",
					"title": "Minio Endpoint",
					"description": "Minio endpoint.",
					"format": "uri",
					"uihints": {
						"ui:placeholder": "minio-service.argo-events:9000"
					}
				},
				"accesskey": {
					"type": "string",
					"title": "AccessKey",
					"description": "Minio accesskey.",
					"uihints": {
						"ui:field": "password"
					}
				},
				"secretkey": {
					"type": "string",
					"title": "SecretKey",
					"description": "Minio secretkey.",
					"uihints": {
						"ui:field": "password"
					}
				},
				"minio_event_parameters": {
					"type": "array",
					"title": "Minio Event Parameters",
					"description": "Minio event parameters.",
					"items": {
						"type": "object",
						"properties": {
							"id": {
								"type": "string",
								"title": "Id"
							},
							"name": {
								"type": "string",
								"title": "Name",
								"enum": ["MinioEvent.operate", "MinioEvent.bucket", "MinioEvent.bucket"]
							}
						},
						"allOf": [
							{
								"if": {
        							"properties": {
          								"name": {
           					 				"const": "MinioEvent.operate"
     						  			}
       								}
      							},
								"then": {
        							"properties": {
          								"operate": {
            								"type": "string",
											"title": "Operate",
											"enum": ["==", "!="]
         								},
										"value": {
											"type": "string",
											"title": "Value"
										}
       								},
        							"required": ["value"]
								}
							},
							{
								"if": {
        							"properties": {
          								"name": {
           					 				"const": "MinioEvent.object"
     						  			}
       								}
      							},
								"then": {
        							"properties": {
          								"operate": {
            								"type": "string",
											"title": "Operate",
											"enum": ["prefix", "suffix"]
         								},
										"value": {
											"type": "string",
											"title": "Value"
										}
       								},
        							"required": ["value"]
								}
							}
						]
					},
					"uihints":{
						"items": {
							"id": { "ui:readonly": "true" }
						}
					}
				},
				{% endif %}
				{% if  component.name == "Pipeline Event" %}
				"pipeline_event_parameters": {
					"type": "array",
					"title": "Pipeline Event parameters",
					"description": "Pipeline event parameters.",
					"items": {
						"type": "object",
						"properties": {
							"id": {
								"type": "string",
								"title": "Id"
							},
							"name": {
								"type": "string",
								"title": "Name",
								"enum": ["PipelineEvent.name", "PipelineEvent.operate", "PipelineEvent.label_selector", "PipelineEvent.create_at"]
							}
						},
						"allOf": [
							{
								"if": {
        							"properties": {
          								"name": {
           					 				"const": "PipelineEvent.name"
     						  			}
       								}
      							},
								"then": {
        							"properties": {
          								"operate": {
            								"type": "string",
											"title": "Operate",
											"enum": ["==", "prefix"]
         								},
										"value": {
											"type": "string",
											"title": "Value"
										}
       								},
        							"required": ["value"]
								}
							},
							{
								"if": {
        							"properties": {
          								"name": {
           					 				"const": "PipelineEvent.operate"
     						  			}
       								}
      							},
								"then": {
        							"properties": {
          								"operate": {
            								"type": "string",
											"title": "Operate",
											"enum": ["=="]
         								},
										"value": {
											"type": "string",
											"title": "Value"
										}
       								},
        							"required": ["value"]
								}
							},
							{
								"if": {
        							"properties": {
          								"name": {
           					 				"const": "PipelineEvent.label_selector"
     						  			}
       								}
      							},
								"then": {
        							"properties": {
          								"operate": {
            								"type": "string",
											"title": "Operate",
											"enum": ["=="]
         								},
										"value": {
											"type": "string",
											"title": "Value"
										}
       								},
        							"required": ["value"]
								}
							},
							{
								"if": {
        							"properties": {
          								"name": {
           					 				"const": "PipelineEvent.create_at"
     						  			}
       								}
      							},
								"then": {
        							"properties": {
          								"operate": {
            								"type": "string",
											"title": "Operate",
											"enum": ["before"]
         								},
										"value": {
											"type": "string",
											"title": "Value",
											"format": "datetime"
										}
       								},
        							"required": ["value"]
								}
							}
						]
					},
					"uihints":{
						"items": {
							"id": { "ui:readonly": "true" }
						}
					}
				},
				{% endif %}
				{% if  component.name == "Calendar Event" %}
				"interval": {
					"type": "integer",
					"title": "Interval",
					"description": "Calendar interval.",
					"minimum": 0
				},
				"schedule": {
					"type": "string",
					"title": "Schedule",
					"description": "Calendar schedule."
				}
				{% endif %}
				{% if  component.name != "Calendar Event" %}
				"expression": {
					"type": "string",
					"title": "Expression",
					"description": "{{component.name}} expression."
				}
				{% endif %}
			},
			"required": []
		}
	},
	"required": ["component_parameters"]
}